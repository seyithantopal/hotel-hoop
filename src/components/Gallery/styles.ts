import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {},
  imageWrapper: {
    marginTop: 10,
    alignItems: 'center',
  },
  image: {
    height: 350,
    borderRadius: 10,
    resizeMode: 'contain',
  },
});
