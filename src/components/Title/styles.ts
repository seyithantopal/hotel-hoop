import { StyleSheet } from 'react-native';
import { COLOR_TITLE } from '../../utils/constants/color';

export default StyleSheet.create({
  title: {
    fontSize: 34,
    color: COLOR_TITLE,
  },
});
