import { StyleSheet } from 'react-native';
import { COLOR_DATE_TITLE_TEXT } from '../../utils/constants/color';

export default StyleSheet.create({
  wrapper: {
    width: 1.5,
    height: 40,
    backgroundColor: COLOR_DATE_TITLE_TEXT,
  },
});
